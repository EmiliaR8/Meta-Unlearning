# Meta-Unlearning
Applying machine unlearning to a binary/multiclass continual learning pipeline where we classify malware into the family it belongs to. We add unlearning to the pipeline; using MADAR to select datapoints for replay and then unlearn points that were not selected. We then extend the unlearning to a meta objective. .
